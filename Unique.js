/*
input the array
determine how many strings are in the array
compare each string to every other string
if two are the same return false
if all are different return true
 */



IsUnique(['happy, sad, grumpy, happy']){
    if ( all are unique
)
        return true;
    if else

        return false;

}